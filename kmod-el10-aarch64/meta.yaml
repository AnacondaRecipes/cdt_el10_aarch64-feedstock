package:
  name: kmod-el10-aarch64
  version: 31

source:
  - url: https://download.rockylinux.org/vault/rocky/10.0/BaseOS/aarch64/os/Packages/k/kmod-31-11.el10.aarch64.rpm
    sha256: fe4df0140dc33d3059760aa0982cc15b20f22e4436f1887c88e721ab0b7ac27a
    no_hoist: true
    folder: binary

build:
  skip: True  # [not (linux and aarch64)]
  number: {{ build_number }}
  noarch: generic
  binary_relocation: false
  detect_binary_files_with_prefix: false
  missing_dso_whitelist:
    - '*'

requirements:
  build:
    - sysroot_linux-aarch64 2.39.*
  host:
  run:
    - sysroot_linux-aarch64 2.39.*
    - kmod-libs-el10-aarch64 ==31 *_{{ build_number }}
    - libgcc-ng
    - libxcrypt-el10-aarch64 >=4.4.36 *_{{ build_number }}
    - xz
    - zlib

test:
  commands:
    - test -f ${PREFIX}/aarch64-conda_el10-linux-gnu/sysroot/usr/sbin/modinfo

about:
  home: https://git.kernel.org/pub/scm/utils/kernel/kmod/kmod.git
  license: LGPL-2.1-only  # GPL-2.0-or-later AND GPL-3.0-or-later AND FSFUL AND FSFULLRWD AND LGPL-2.1-only AND LGPL-2.1-or-later AND X11
  license_family: GPL2
  license_file:
    - COPYING
  summary: (CDT) Linux kernel module management utilities
  description: |
    The kmod package provides various programs needed for automatic
    loading and unloading of modules under 2.6, 3.x, and later kernels, as well
    as other module management programs. Device drivers and filesystems are two
    examples of loaded and unloaded modules.
  doc_url: https://git.kernel.org/pub/scm/utils/kernel/kmod/kmod.git
  dev_url: https://git.kernel.org/pub/scm/utils/kernel/kmod/kmod.git

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
